import { TextField } from "@mui/material";

const SearchTextField = () => {
  return (
    <TextField
      id="outlined-basic"
      label="search"
      variant="outlined"
      size="small"
    />
  );
};

export default SearchTextField;
