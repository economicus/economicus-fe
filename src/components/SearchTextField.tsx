import { TextField } from "@material-ui/core";

const SearchTextField = () => {
  return (
    <TextField
      id="outlined-basic"
      label="search"
      variant="outlined"
      size="small"
    />
  );
};

export default SearchTextField;
